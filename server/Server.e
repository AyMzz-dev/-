.版本 2
.程序集 窗口程序集_启动窗口
.程序集变量 服务器, 网络服务器
.程序集变量 数据库, Sqlite数据库
.程序集变量 端口, 整数型
.程序集变量 数据库路径, 文本型

.子程序 __启动窗口_创建完毕
    ' 初始化界面
    编辑框_端口.内容 ＝ "99"
    
    ' 数据库文件默认放在程序同目录
    数据库路径 ＝ 取运行目录 () ＋ "\auth.db"
    编辑框_数据库路径.内容 ＝ 数据库路径
    
    状态条1.置文本 (0, "就绪 - 等待启动")
    按钮_停止.禁止 ＝ 真

.子程序 _按钮_启动_被单击
    ' 连接数据库
    如果真 (数据库.是否打开 ())
        数据库.关闭 ()
    如果真结束
    
    数据库路径 ＝ 编辑框_数据库路径.内容
    如果 (数据库.打开 (数据库路径, 假))
        添加日志 ("[数据库] 连接成功 " ＋ 数据库路径)
        状态条1.置文本 (0, "数据库已连接")
        创建数据表 ()
    否则
        添加日志 ("[错误] 数据库打开失败！")
        信息框 ("数据库打开失败，请检查路径！", 16, , )
        返回 ()
    如果结束
    
    ' 启动HTTP服务器
    端口 ＝ 到整数 (编辑框_端口.内容)
    如果 (服务器.启动 (端口, &请求处理函数))
        添加日志 ("[服务器] 启动成功 HTTP://0.0.0.0:" ＋ 到文本 (端口))
        状态条1.置文本 (0, "服务运行中 - 端口:" ＋ 到文本 (端口))
        按钮_启动.禁止 ＝ 真
        按钮_停止.禁止 ＝ 假
    否则
        添加日志 ("[错误] 服务器启动失败！端口可能被占用")
        状态条1.置文本 (0, "启动失败")
    如果结束

.子程序 _按钮_停止_被单击
    服务器.停止 ()
    添加日志 ("[服务器] 已停止")
    状态条1.置文本 (0, "已停止")
    按钮_启动.禁止 ＝ 假
    按钮_停止.禁止 ＝ 真

.子程序 __启动窗口_将被销毁
    如果 (服务器.是否运行 ())
        服务器.停止 ()
    如果结束
    如果 (数据库.是否打开 ())
        数据库.关闭 ()
    如果结束

.子程序 添加日志
.参数 内容, 文本型
    编辑框_日志.加入文本 (内容 ＋ #换行符)
    编辑框_日志.起始选择位置 ＝ 取文本长度 (编辑框_日志.内容)
    编辑框_日志.滚动到尾部 ()

.子程序 创建数据表
    ' 授权站点表
    数据库.执行SQL语句 (“CREATE TABLE IF NOT EXISTS auth_sites (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        domain TEXT NOT NULL,
        sitekey TEXT NOT NULL UNIQUE,
        status INTEGER DEFAULT 1,
        note TEXT DEFAULT '',
        create_time INTEGER NOT NULL,
        last_check_time INTEGER DEFAULT 0
    )”)
    
    ' 授权日志表
    数据库.执行SQL语句 (“CREATE TABLE IF NOT EXISTS auth_logs (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        sitekey TEXT NOT NULL,
        domain TEXT NOT NULL,
        action TEXT NOT NULL,
        ip TEXT NOT NULL,
        result INTEGER DEFAULT 0,
        create_time INTEGER NOT NULL
    )”)
    
    添加日志 (“[数据库] 数据表初始化完成”)

.子程序 请求处理函数
.参数 请求数据, 文本型
.参数 客户端IP, 文本型
.局部变量 返回数据, 文本型
.局部变量 GET参数, 文本型
.局部变量 POST数据, 文本型
.局部变量 模块名, 文本型
.局部变量 域名, 文本型
.局部变量 站点密钥, 文本型

    ' 解析HTTP请求
    如果真 (取文本长度 (请求数据) ＜ 10)
        返回数据 ＝ 生成HTTP响应 (“400 Bad Request”, “请求无效”)
        服务器.发送数据 (客户端IP, 返回数据)
        返回 ()
    如果真结束
    
    ' 提取GET参数
    GET参数 ＝ 提取GET参数 (请求数据)
    POST数据 ＝ 提取POST数据 (请求数据)
    
    模块名 ＝ 取参数值 (GET参数, “mod”)
    域名 ＝ 取参数值 (GET参数, “domain”)
    站点密钥 ＝ 取参数值 (GET参数, “sitekey”)
    
    添加日志 (“[请求] ” ＋ 客户端IP ＋ “ → mod=” ＋ 模块名 ＋ “ domain=” ＋ 域名)
    
    ' 路由分发
    判断 (模块名 ＝ “checkauth”)
        返回数据 ＝ 处理验证授权 (域名, 站点密钥, 客户端IP)
    判断 (模块名 ＝ “sendmail”)
        返回数据 ＝ 处理发送邮件 (域名, 站点密钥, 客户端IP, POST数据)
    默认
        返回数据 ＝ 生成HTTP响应 (“200 OK”, 生成JSON (0, “未知模块”, “”))
    判断结束
    
    服务器.发送数据 (客户端IP, 返回数据)

.子程序 处理验证授权
.参数 域名, 文本型
.参数 站点密钥, 文本型
.参数 客户端IP, 文本型
.局部变量 结果, 整数型
.局部变量 SQL, 文本型
.局部变量 记录集, 整数型
.局部变量 状态, 整数型
.局部变量 提示, 文本型
.局部变量 返回JSON, 文本型

    ' 参数校验
    如果真 (域名 ＝ “” 或 站点密钥 ＝ “”)
        返回 (生成HTTP响应 (“200 OK”, 生成JSON (0, “参数不完整”, “”)))
    如果真结束
    
    ' 查询授权
    SQL ＝ “SELECT status FROM auth_sites WHERE sitekey='” ＋ 站点密钥 ＋ “' AND domain='” ＋ 域名 ＋ “'”
    记录集 ＝ 数据库.查询 (SQL)
    
    如果 (记录集 ＞ 0)
        数据库.读字段值 (记录集, “status”, 状态)
        数据库.关闭记录集 (记录集)
        
        如果 (状态 ＝ 1)
            ' 授权有效
            返回JSON ＝ 生成JSON (1, “授权有效”, “”)
            结果 ＝ 1
            添加日志 (“[验证] 通过 - ” ＋ 域名)
        否则
            ' 授权被禁用
            提示 ＝ “您的授权已被封禁，请联系管理员”
            返回JSON ＝ 生成JSON (-3, 提示, 提示)
            结果 ＝ -3
            添加日志 (“[验证] 被禁用 - ” ＋ 域名)
        如果结束
    否则
        ' 授权不存在
        返回JSON ＝ 生成JSON (0, “授权不存在”, “域名或密钥不匹配”)
        结果 ＝ 0
        添加日志 (“[验证] 未授权 - ” ＋ 域名)
    如果结束
    
    ' 记录日志
    SQL ＝ “INSERT INTO auth_logs (sitekey,domain,action,ip,result,create_time) VALUES ('” ＋ 站点密钥 ＋ “','” ＋ 域名 ＋ “','checkauth','” ＋ 客户端IP ＋ “',” ＋ 到文本 (结果) ＋ “,” ＋ 到文本 (取时间戳 ()) ＋ “)”
    数据库.执行SQL语句 (SQL)
    
    ' 更新最后验证时间
    SQL ＝ “UPDATE auth_sites SET last_check_time=” ＋ 到文本 (取时间戳 ()) ＋ “ WHERE sitekey='” ＋ 站点密钥 ＋ “'”
    数据库.执行SQL语句 (SQL)
    
    返回 (生成HTTP响应 (“200 OK”, 返回JSON))

.子程序 处理发送邮件
.参数 域名, 文本型
.参数 站点密钥, 文本型
.参数 客户端IP, 文本型
.参数 POST数据, 文本型
.局部变量 标题, 文本型
.局部变量 内容, 文本型
.局部变量 收件人, 文本型
.局部变量 返回JSON, 文本型
.局部变量 结果, 整数型

    ' 参数校验
    如果真 (域名 ＝ “” 或 站点密钥 ＝ “”)
        返回 (生成HTTP响应 (“200 OK”, 生成JSON (0, “参数不完整”, “”)))
    如果真结束
    
    标题 ＝ 取参数值 (POST数据, “title”)
    内容 ＝ 取参数值 (POST数据, “content”)
    收件人 ＝ 取参数值 (POST数据, “tomail”)
    
    添加日志 (“[邮件] 收件人=” ＋ 收件人 ＋ “ 标题=” ＋ 标题)
    
    ' 邮件发送功能（可选，需要配置SMTP）
    ' 暂时返回成功
    
    返回JSON ＝ 生成JSON (1, “发送成功”, “”)
    结果 ＝ 1
    
    ' 记录日志
    数据库.执行SQL语句 (“INSERT INTO auth_logs (sitekey,domain,action,ip,result,create_time) VALUES ('” ＋ 站点密钥 ＋ “','” ＋ 域名 ＋ “','sendmail','” ＋ 客户端IP ＋ “',” ＋ 到文本 (结果) ＋ “,” ＋ 到文本 (取时间戳 ()) ＋ “)”)
    
    返回 (生成HTTP响应 (“200 OK”, 返回JSON))

.子程序 生成JSON
.参数 Code, 整数型
.参数 Msg, 文本型
.参数 Tips, 文本型
.局部变量 结果, 文本型

    结果 ＝ “{” ＋ #引号 ＋ “code” ＋ #引号 ＋ “:” ＋ 到文本 (Code)
    如果真 (Msg ≠ “”)
        结果 ＝ 结果 ＋ “,” ＋ #引号 ＋ “msg” ＋ #引号 ＋ “:” ＋ #引号 ＋ Msg ＋ #引号
    如果真结束
    如果真 (Tips ≠ “”)
        结果 ＝ 结果 ＋ “,” ＋ #引号 ＋ “tips” ＋ #引号 ＋ “:” ＋ #引号 ＋ Tips ＋ #引号
    如果真结束
    结果 ＝ 结果 ＋ “}”
    
    返回 (结果)

.子程序 生成HTTP响应
.参数 状态描述, 文本型
.参数 内容, 文本型
.局部变量 响应, 文本型

    响应 ＝ “HTTP/1.1 ” ＋ 状态描述 ＋ #换行符
    响应 ＝ 响应 ＋ “Content-Type: application/json; charset=utf-8” ＋ #换行符
    响应 ＝ 响应 ＋ “Content-Length: ” ＋ 到文本 (取文本长度 (内容)) ＋ #换行符
    响应 ＝ 响应 ＋ “Access-Control-Allow-Origin: *” ＋ #换行符
    响应 ＝ 响应 ＋ “Connection: close” ＋ #换行符
    响应 ＝ 响应 ＋ #换行符
    响应 ＝ 响应 ＋ 内容
    
    返回 (响应)

.子程序 提取GET参数
.参数 请求数据, 文本型
.局部变量 行数组, 文本型, , "0"
.局部变量 第一行, 文本型
.局部变量 参数部分, 文本型
.局部变量 位置, 整数型

    行数组 ＝ 分割文本 (请求数据, #换行符, )
    如果真 (取数组成员数 (行数组) ＜ 1)
        返回 (“”)
    如果真结束
    
    第一行 ＝ 行数组 [1]
    
    ' 提取 GET /api.php?xxx 中的参数部分
    位置 ＝ 寻找文本 (第一行, “?”, , 假)
    如果真 (位置 ＝ -1)
        返回 (“”)
    如果真结束
    
    参数部分 ＝ 取文本右边 (第一行, 取文本长度 (第一行) － 位置)
    
    ' 去掉 HTTP/1.1 部分
    位置 ＝ 寻找文本 (参数部分, “ HTTP”, , 假)
    如果真 (位置 ＞ 0)
        参数部分 ＝ 取文本左边 (参数部分, 位置 － 1)
    如果真结束
    
    返回 (参数部分)

.子程序 提取POST数据
.参数 请求数据, 文本型
.局部变量 位置, 整数型

    ' 找到空行，后面就是POST数据
    位置 ＝ 寻找文本 (请求数据, #换行符 ＋ #换行符, , 假)
    如果真 (位置 ＝ -1)
        返回 (“”)
    如果真结束
    
    返回 (取文本右边 (请求数据, 取文本长度 (请求数据) － 位置 － 2))

.子程序 取参数值
.参数 参数字符串, 文本型
.参数 参数名, 文本型
.局部变量 参数数组, 文本型, , "0"
.局部变量 键值数组, 文本型, , "0"
.局部变量 i, 整数型

    如果真 (参数字符串 ＝ “”)
        返回 (“”)
    如果真结束
    
    参数数组 ＝ 分割文本 (参数字符串, “&”, )
    计次循环首 (取数组成员数 (参数数组), i)
        键值数组 ＝ 分割文本 (参数数组 [i], “=”, )
        如果真 (取数组成员数 (键值数组) ≥ 2)
            如果真 (键值数组 [1] ＝ 参数名)
                返回 (URL解码 (键值数组 [2]))
            如果真结束
        如果真结束
    计次循环尾 ()
    
    返回 (“”)

.子程序 URL解码
.参数 编码文本, 文本型
.局部变量 结果, 文本型
.局部变量 i, 整数型
.局部变量 当前字符, 文本型
.局部变量 十六进制, 文本型

    结果 ＝ “”
    计次循环首 (取文本长度 (编码文本), i)
        当前字符 ＝ 取文本中间 (编码文本, i, 1)
        如果 (当前字符 ＝ “+”)
            结果 ＝ 结果 ＋ “ ”
        否则如果 (当前字符 ＝ “%” 且 i ＋ 2 ≤ 取文本长度 (编码文本))
            十六进制 ＝ 取文本中间 (编码文本, i ＋ 1, 2)
            结果 ＝ 结果 ＋ 字符 (十六进制到十进制 (十六进制))
            i ＝ i ＋ 2
        否则
            结果 ＝ 结果 ＋ 当前字符
        如果结束
    计次循环尾 ()
    
    返回 (结果)

.子程序 十六进制到十进制
.参数 十六进制, 文本型
.局部变量 结果, 整数型
.局部变量 字符, 文本型
.局部变量 值, 整数型
.局部变量 i, 整数型

    结果 ＝ 0
    计次循环首 (取文本长度 (十六进制), i)
        字符 ＝ 取文本中间 (十六进制, i, 1)
        值 ＝ 寻找文本 (“0123456789ABCDEF”, 到大写 (字符), , 假)
        如果真 (值 ＝ -1)
            值 ＝ 寻找文本 (“0123456789abcdef”, 字符, , 假)
        如果真结束
        如果真 (值 ＞ 0)
            值 ＝ 值 － 1
        如果真结束
        结果 ＝ 结果 × 16 ＋ 值
    计次循环尾 ()
    
    返回 (结果)

.子程序 取时间戳
    ' 返回当前Unix时间戳
    返回 (到整数 (取时间间隔 (取现行时间 (), [1970年1月1日], 8)))

.子程序 _按钮_添加授权_被单击
.局部变量 域名, 文本型
.局部变量 站点密钥, 文本型
.局部变量 备注, 文本型
.局部变量 SQL, 文本型

    域名 ＝ 编辑框_添加域名.内容
    站点密钥 ＝ 编辑框_添加密钥.内容
    备注 ＝ 编辑框_添加备注.内容
    
    如果真 (域名 ＝ “” 或 站点密钥 ＝ “”)
        信息框 (“域名和密钥不能为空！”, 16, , )
        返回 ()
    如果真结束
    
    如果真 (取文本长度 (站点密钥) ＜ 16)
        信息框 (“密钥长度至少16位，建议使用64位随机字符串！”, 48, , )
        返回 ()
    如果真结束
    
    SQL ＝ “INSERT INTO auth_sites (domain,sitekey,status,note,create_time) VALUES ('” ＋ 域名 ＋ “','” ＋ 站点密钥 ＋ “',1,'” ＋ 备注 ＋ “',” ＋ 到文本 (取时间戳 ()) ＋ “)”
    
    如果 (数据库.执行SQL语句 (SQL))
        添加日志 (“[授权] 添加成功 - ” ＋ 域名)
        信息框 (“授权添加成功！请将密钥复制给客户。” ＋ #换行符 ＋ #换行符 ＋ “站点密钥: ” ＋ 站点密钥, 64, , )
        编辑框_添加域名.内容 ＝ “”
        编辑框_添加密钥.内容 ＝ “”
        编辑框_添加备注.内容 ＝ “”
        刷新授权列表 ()
    否则
        添加日志 (“[错误] 添加授权失败，可能域名或密钥已存在”)
        信息框 (“添加失败！请检查域名和密钥是否已存在。”, 16, , )
    如果结束

.子程序 _按钮_生成密钥_被单击
.局部变量 密钥, 文本型
.局部变量 字符表, 文本型
.局部变量 i, 整数型
.局部变量 随机位置, 整数型

    字符表 ＝ “ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789”
    密钥 ＝ “”
    计次循环首 (64, i)
        随机位置 ＝ 取随机数 (1, 取文本长度 (字符表))
        密钥 ＝ 密钥 ＋ 取文本中间 (字符表, 随机位置, 1)
    计次循环尾 ()
    编辑框_添加密钥.内容 ＝ 密钥

.子程序 刷新授权列表
.局部变量 SQL, 文本型
.局部变量 记录集, 整数型
.局部变量 域名, 文本型
.局部变量 站点密钥, 文本型
.局部变量 状态, 整数型
.局部变量 备注, 文本型
.局部变量 创建时间, 整数型
.局部变量 最后验证, 整数型
.局部变量 索引, 整数型

    超级列表框_授权列表.全部删除 ()
    
    SQL ＝ “SELECT * FROM auth_sites ORDER BY id DESC”
    记录集 ＝ 数据库.查询 (SQL)
    
    判断循环首 (数据库.到下一行 (记录集))
        数据库.读字段值 (记录集, “domain”, 域名)
        数据库.读字段值 (记录集, “sitekey”, 站点密钥)
        数据库.读字段值 (记录集, “status”, 状态)
        数据库.读字段值 (记录集, “note”, 备注)
        数据库.读字段值 (记录集, “create_time”, 创建时间)
        数据库.读字段值 (记录集, “last_check_time”, 最后验证)
        
        索引 ＝ 超级列表框_授权列表.插入表项 (, 域名, , , , )
        超级列表框_授权列表.置标题 (索引, 1, 取文本左边 (站点密钥, 8) ＋ “...”)
        超级列表框_授权列表.置标题 (索引, 2, 选择 (状态 ＝ 1, “正常”, “禁用”))
        超级列表框_授权列表.置标题 (索引, 3, 备注)
        超级列表框_授权列表.置标题 (索引, 4, 时间戳到文本 (创建时间))
        超级列表框_授权列表.置标题 (索引, 5, 选择 (最后验证 ＞ 0, 时间戳到文本 (最后验证), “从未验证”))
    判断循环尾 ()
    数据库.关闭记录集 (记录集)

.子程序 时间戳到文本
.参数 时间戳, 整数型
.局部变量 时间, 日期时间型

    时间 ＝ 增减时间 ([1970年1月1日], 8, 时间戳)
    返回 (到文本 (时间))

.子程序 _超级列表框_授权列表_右键单击
.参数 行索引, 整数型
.局部变量 菜单, 弹出菜单

    如果真 (行索引 ＝ -1)
        返回 ()
    如果真结束
    
    菜单.添加菜单项 (“启用”, , &启用授权, )
    菜单.添加菜单项 (“禁用”, , &禁用授权, )
    菜单.添加菜单项 (“-”, , , )
    菜单.添加菜单项 (“删除”, , &删除授权, )
    菜单.弹出菜单 (, , )

.子程序 启用授权
.局部变量 行索引, 整数型
.局部变量 域名, 文本型

    行索引 ＝ 超级列表框_授权列表.现行选中项
    如果真 (行索引 ＝ -1)
        返回 ()
    如果真结束
    域名 ＝ 超级列表框_授权列表.取标题 (行索引, 0)
    数据库.执行SQL语句 (“UPDATE auth_sites SET status=1 WHERE domain='” ＋ 域名 ＋ “'”)
    刷新授权列表 ()
    添加日志 (“[授权] 已启用 - ” ＋ 域名)

.子程序 禁用授权
.局部变量 行索引, 整数型
.局部变量 域名, 文本型

    行索引 ＝ 超级列表框_授权列表.现行选中项
    如果真 (行索引 ＝ -1)
        返回 ()
    如果真结束
    域名 ＝ 超级列表框_授权列表.取标题 (行索引, 0)
    如果 (信息框 (“确定要禁用 ” ＋ 域名 ＋ “ 的授权吗？禁用后客户端将无法使用！”, 36, “确认禁用”, ))
        数据库.执行SQL语句 (“UPDATE auth_sites SET status=0 WHERE domain='” ＋ 域名 ＋ “'”)
        刷新授权列表 ()
        添加日志 (“[授权] 已禁用 - ” ＋ 域名)
    如果结束

.子程序 删除授权
.局部变量 行索引, 整数型
.局部变量 域名, 文本型

    行索引 ＝ 超级列表框_授权列表.现行选中项
    如果真 (行索引 ＝ -1)
        返回 ()
    如果真结束
    域名 ＝ 超级列表框_授权列表.取标题 (行索引, 0)
    如果 (信息框 (“确定要删除 ” ＋ 域名 ＋ “ 的授权吗？此操作不可恢复！”, 52, “确认删除”, ))
        数据库.执行SQL语句 (“DELETE FROM auth_sites WHERE domain='” ＋ 域名 ＋ “'”)
        刷新授权列表 ()
        添加日志 (“[授权] 已删除 - ” ＋ 域名)
    如果结束