# 服务器部署清单 — 2026.07.05

## 一、修复登录 Bug（紧急）

| 文件 | 上传路径 | 说明 |
|------|----------|------|
| `function/function_core.php` | `/www/wwwroot/auth.authcnmtx.cn/function/` | 缺少 verify_password()/hash_password() 函数 |

## 二、导航栏 + 新页面

| 文件 | 上传路径 | 说明 |
|------|----------|------|
| `Adminx/admin.html` | `/www/wwwroot/auth.authcnmtx.cn/Adminx/` | 侧边栏新增公告管理、更新中心 |
| `Adminx/ajax.php` | `/www/wwwroot/auth.authcnmtx.cn/Adminx/` | 新增 getnotice/getuplog/getversion/checkupdate 等 API |
| `Adminx/html/gggl.html` | `/www/wwwroot/auth.authcnmtx.cn/Adminx/html/` | 公告管理页面 |
| `Adminx/html/gxzx.html` | `/www/wwwroot/auth.authcnmtx.cn/Adminx/html/` | 更新中心页面 |
| `Adminx/html/welcome.html` | `/www/wwwroot/auth.authcnmtx.cn/Adminx/html/` | 欢迎页新增系统更新面板 |
| `admin/admin.html` | `/www/wwwroot/auth.authcnmtx.cn/admin/` | 二级后台同步 |
| `admin/ajax.php` | `/www/wwwroot/auth.authcnmtx.cn/admin/` | 二级后台同步 API |
| `admin/html/gggl.html` | `/www/wwwroot/auth.authcnmtx.cn/admin/html/` | 二级后台公告管理 |
| `admin/html/gxzx.html` | `/www/wwwroot/auth.authcnmtx.cn/admin/html/` | 二级后台更新中心 |
| `admin/html/welcome.html` | `/www/wwwroot/auth.authcnmtx.cn/admin/html/` | 二级后台欢迎页 |

## 三、数据库迁移

在服务器数据库执行以下 SQL：

```sql
CREATE TABLE IF NOT EXISTS `sq_notice` (
  `ID` int(11) NOT NULL AUTO_INCREMENT,
  `title` varchar(255) NOT NULL,
  `content` text NOT NULL,
  `time` int(11) NOT NULL,
  `status` int(1) NOT NULL DEFAULT '1',
  `sort` int(11) NOT NULL DEFAULT '0',
  PRIMARY KEY (`ID`) USING BTREE
) ENGINE=InnoDB DEFAULT CHARSET=utf8 ROW_FORMAT=COMPACT;

INSERT INTO `sq_notice` (`ID`, `title`, `content`, `time`, `status`, `sort`) VALUES
(1, '温泉PHP授权系统 v12.12 小七二次开发版发布', '欢迎使用温泉PHP网络授权系统 v12.12！\n\n本版本由小七二次开发，主要更新内容：\n1. 数据库表分离，一级/二级管理员独立管理\n2. 密码安全升级，使用 password_hash 加密\n3. 新增云黑等级系统、云白系统\n4. 新增公告管理、在线更新中心\n5. 界面全面美化，紫蓝渐变玻璃态设计\n\n开源地址：https://github.com/AyMzz-dev/-', UNIX_TIMESTAMP(), 1, 10),
(2, '关于系统开源说明', '本系统采用 AGPL-3.0 开源协议发布。\n\n您可以自由使用、修改和分发本软件，但如果您修改后通过网络提供服务，必须公开修改后的源代码。\n\n严谨用于任何违反中华人民共和国法律法规的用途。\n\n感谢您的使用！', UNIX_TIMESTAMP(), 1, 5);
```

## 四、上传顺序

1. **先上传** `function/function_core.php`（修复登录，否则无法进入后台）
2. **上传** `Adminx/` 和 `admin/` 下所有列出的文件
3. **执行** 数据库 SQL
4. 刷新 `http://auth.authcnmtx.cn/Adminx/admin.html` 验证